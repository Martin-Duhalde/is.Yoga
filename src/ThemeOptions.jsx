// ThemeOptions.jsx
import { createTheme } from "@mui/material/styles";

export const themeOptions = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#8b3fb5",
    },
    secondary: {
      main: "#f50057",
    },
  },
});
